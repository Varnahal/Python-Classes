def area(l,c):
    print(f'O terreno com a largura{l} e comprimento {c} tem area {l*c:.2f}')

largura = float(input('digite a largura do terreno: '))
comprimebnto = float(input('digite a comprimebnto do terreno: '))

area(largura,comprimebnto)